# wordpress-docker-init

To use it, simple run the following: 

```
git clone https://github.com/andrescalco/wordpress-docker-init.git folder-name
cd  folder-name
mkdir site db
docker-compose up --build
```
